version: "3.9"
services:
  pipeline:
    build: .
    environment:
      - INGESTION_DATE=${INGESTION_DATE:-2024-01-01}
    volumes:
      - ./:/app
    command: ["python", "src/main.py", "run-pipeline", "--ingestion-date", "${INGESTION_DATE:-2024-01-01}"]
